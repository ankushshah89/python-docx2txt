#! /usr/bin/env python

import pydocx

if __name__ == '__main__':
    import sys
    args = pydocx.process_args()
    text = pydocx.process(args.docx, args.img_dir)
    sys.stdout.write(text.encode('utf-8'))
